# Agent Specifications

Each agent is an ADK `LlmAgent` (via `Agent`) with explicit instructions, tools, and optional sub-agents. Orchestration uses ADK Runner sessions rather than custom message handlers.
