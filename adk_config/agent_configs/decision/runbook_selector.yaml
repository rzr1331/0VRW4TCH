# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: runbook_selector
model: gemini-2.5-flash-lite
description: Selects the best runbook for a given situation.
instruction: |
  You are the runbook selector agent in the decision layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
