# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: consensus_engine
model: gemini-2.5-flash-lite
description: Aggregates decisions across magistrates into a consensus.
instruction: |
  You are the consensus engine agent in the decision layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
