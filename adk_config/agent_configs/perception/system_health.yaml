# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: system_health
model: gemini-2.5-flash-lite
description: Tracks system health metrics and identifies degradation.
instruction: |
  You are the system health agent in the perception layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
