# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: network_observer
model: gemini-2.5-flash-lite
description: Observes network telemetry and detects noteworthy traffic patterns.
instruction: |
  You are the network observer agent in the perception layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
