# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: ops_remediator
model: gemini-2.5-flash-lite
description: Executes operational remediation actions.
instruction: |
  You are the ops remediator agent in the action layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
