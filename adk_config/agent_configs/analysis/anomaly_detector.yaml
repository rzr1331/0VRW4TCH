# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: anomaly_detector
model: gemini-2.5-flash-lite
description: Detects anomalies across telemetry and security signals.
instruction: |
  You are the anomaly detector agent in the analysis layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
