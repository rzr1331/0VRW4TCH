# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
agent_class: LlmAgent
name: root_cause_analyzer
model: gemini-2.5-flash-lite
description: Analyzes incidents to identify likely root causes.
instruction: |
  You are the root cause analyzer agent in the analysis layer. Follow the system's guardrails and provide concise, structured outputs. If you need more context, request it explicitly.
